---
id: quick-001
title: Create a new Python app skeleton
status: todo
priority: high
labels: [quickstart, scaffold]
git:
  base_branch: main
  work_branch: agent/test-agent/quick-001
  push_on_success: false
steps:
  - id: create-structure
    type: shell
    tool_preferences: [shell]
    command: python -c "from pathlib import Path; Path('src/my_app').mkdir(parents=True, exist_ok=True); Path('tests').mkdir(parents=True, exist_ok=True); Path('src/my_app/__init__.py').write_text('__version__ = \"0.1.0\"\\n', encoding='utf-8'); Path('src/my_app/main.py').write_text('def main():\\n    print(\"Hello from my_app\")\\n\\nif __name__ == \"__main__\":\\n    main()\\n', encoding='utf-8'); Path('tests/test_smoke.py').write_text('def test_smoke():\\n    assert True\\n', encoding='utf-8')"
  - id: add-readme
    type: shell
    tool_preferences: [shell]
    command: python -c "from pathlib import Path; Path('README.md').write_text('# my_app\\n\\nGenerated by execforge\\n', encoding='utf-8')"
  - id: summarize
    type: llm_summary
    tool_preferences: [mock]
    prompt_inline: Summarize what was created for the new project skeleton.
---

Create a minimal Python project skeleton quickly.
